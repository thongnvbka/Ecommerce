@model PayReceivablesMeta
@{
    ViewBag.Title = "Add new debt settlement";
}
<script>
    var payReceivableJsTree = @Html.Raw(ViewBag.payReceivableJsTree);
</script>
<section class="content-header">
    <h1>
        Add new debt settlement
    </h1>
    <ol class="breadcrumb">
        <li><a href="/"><i class="fa fa-dashboard"></i>Home</a></li>
        <li><a href="@Url.Action("AccountingPayReceivable")">List of debt obligations</a></li>
        <li class="active">Add new debt settlement</li>
    </ol>
</section>
<section class="content">
    <div class="box box-info">
        <form action="@Url.Action("CreatePayReceivable")" method="post" class="form-horizontal" id="submitForm">
            @Html.AntiForgeryToken()
            <div class="box-body">
                @Html.Partial("_UpdatePayReceivable", Model)
            </div>
            <div class="box-footer">
                <a href="@Url.Action("AccountingPayReceivable")" role="button" class="btn btn-default"><i class="fa fa-arrow-left"></i> Back to list</a>
                <button type="button" class="btn btn-info pull-right" onclick="submitForm()"><i class="fa fa-plus"></i> Add a term</button>
            </div>
        </form>
    </div>
</section>

